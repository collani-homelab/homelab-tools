[project]
name = "agent-vision-patrol"
version = "0.1.0"
description = "Vision patrol agent — screenshots Grafana panels and uses a multimodal LLM to detect anomalies."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.32.0",
]
